# myHabbit-game 12.0.1 — PR deploy update

Цей реліз створений як окреме оновлення для запуску деплою через Pull Request.

## Виправлено

- відновлено коректне динамічне підключення `public/app.js`;
- додано обробник помилки завантаження головного JavaScript-модуля;
- оновлено версію застосунку та PWA-кеш до `12.0.1`;
- примусово оновлюється URL `app.js`, щоб браузер і Service Worker не використовували пошкоджену стару копію.
