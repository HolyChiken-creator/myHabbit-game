# Pull Request: myHabbit-game 12.0.2

Apply this update-only archive over branch based on 12.0.1, commit, push, then merge the PR into `main`.

Main change: first visual cleanup pass for the home room. The concept image with baked UI is no longer used as the runtime background.
