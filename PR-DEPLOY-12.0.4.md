# myHabbit-game 12.0.4 — Mobile-first room rebuild

Apply this update over 12.0.3 in a new branch and open a Pull Request into `main`.

Main goal: replace the compressed desktop room on phones with a dedicated mobile composition. The room scene occupies the upper area, while daily tasks are rendered in a clean vertical panel below it. Includes safe-area and landscape fixes.
