# Integration map

Copy `public/assets/generated/pack_01_home_screen_backgrounds/` over the current project. Select responsive files from `webp/portrait`, `webp/tablet`, or `webp/desktop`. Use `background-size: cover; background-position: center;`. Do not replace current backgrounds until the state mapper is connected to manifest IDs.
